# Reports

Reports summarize important tasks without duplicating full result tables. The default naming rule is `report_<YYYYMMDD-HHMM>_<snake_case_task_name>.md` using Asia/Shanghai time. Figures should be linked from `../results/figures/` and include short captions.

